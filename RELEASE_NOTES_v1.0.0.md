> [!IMPORTANT]
> **本ツールは必ず「管理者として実行」してください。**
> 使い方・既知の制限は [README](https://github.com/Defoco303/resonance-midi#readme) を必ずご確認ください。

## v1.0.0

**機能を積み終えた版として 1.0.0 にしました。** 新機能の追加はありません。今回は**安定性の修正だけ**です。これ以降は、実際に使って見つかった不具合の修正を優先します。

実機検証を終えた**スマートアレンジ・表情演奏・ドラムは、ベータ表記を外しました**。音域補正とギター・ベースは、確認しきれていない部分（ピアノの C7～C8、ベースの音域切替）があるためベータのままです。詳しくは README の「v1.0.0の既知の制限」を参照してください。

### 直した不具合

**壊れたMIDIが混ざっていると、別の曲が鳴っていました。**
プレイリストの再生中に読み込めないMIDIへ進むと、曲名の表示だけが次の曲に変わり、**実際には前の曲がもう一度鳴っていました**。読み込みに失敗した曲は飛ばして次へ進むようにしました。

**キー入力に失敗すると、以降まったく再生できなくなることがありました。**
演奏中にユーザーアカウント制御（UAC）の画面やロック画面が出ると、Windowsがキー入力を受け付けなくなります。このときツールの内部で演奏スレッドが停止し、**エラーも出ないまま、再生ボタンを押しても何も起きない状態**になっていました。エラーを表示したうえで、次の再生から復帰できるようにしました。

**`Shift` や `Ctrl` が押されたままになることがありました。**
音域切替のキーを離す操作が失敗すると、ゲームだけでなくWindows全体で修飾キーが押しっぱなしになっていました。離すのに失敗したキーを覚えておき、停止・シーク・終了のたびに離し直すようにしました。同じ理由で音が鳴りっぱなしになる問題も直しています。

**キー入力に失敗すると、和音の音数だけ警告が出ていました。**
4音の和音で1回失敗すると、**同じ警告が4回続けて表示**されていました。最初の失敗でその和音の処理を止め、警告は1回だけにしました。

**設定ファイルが壊れると、起動しなくなっていました。**
`config.json` の値の型が壊れていると、ウィンドウが表示される前にエラーになり、**画面には何も出ないまま終了**していました。壊れた値だけを初期値へ戻して起動を続けるようにし、それでも起動できない場合は原因を表示するようにしました。

**設定の壊れ方によっては、正常な設定まで初期値に戻っていました。**
`config.json` の中の1か所（設定ファイルの版数）が読めないと、**ほかの正常な設定もすべて初期値へ戻って**いました。壊れた値だけを戻すようにしています。あわせて、`"false"` という文字列が「オン」と解釈される問題と、数値として無効な値（`NaN`・無限大）が通ってしまう問題も直しました。

**合奏の開始時刻が最大5%ずれることがありました。**
開始を予約するタイマーが大まかな精度で動いており、30秒以上先を指定すると1秒近くずれる可能性がありました。正確なタイマーへ変更しています。

**拍子が変わる曲で、拍の位置を二重に数えていました。**
4/4から3/4へ変わるような曲で、変わり目の1拍を2回数えていました。表情演奏がフレーズの切れ目や強拍を判断する材料になるため、修正しています。

**MIDI書き出しの「鳴らないため除外した音」が多すぎました。**
トラックを選んで演奏している場合、**選ばなかったトラックの音まで「鳴らない音」として数えて**いました。表示だけの問題で、演奏される内容は変わりません。

### 内部の変更

- 読み込みに失敗した曲が、ツール内部に残らないようにしました（画面と再生側で認識がずれていました）。
- 配布EXEのビルドに使うNuitkaのバージョンを固定しました（Python 3.13.5 / Nuitka 4.1.3 / PySide6 6.11.1）。指定と違う版が入っている場合はビルドを中止します。
- 使われなくなっていた旧UI（Tkinter版）と旧ビルド設定を削除しました。
- 自動テストを 295 件から 334 件へ増やしました。

### 不具合の報告

動作報告・不具合の報告は、README の「報告・お問い合わせ」からお願いします。
