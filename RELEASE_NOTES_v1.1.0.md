> [!IMPORTANT]
> **本ツールは必ず「管理者として実行」してください。**
> 使い方・公開演奏の限界・既知の制限は [README](https://github.com/Defoco303/resonance-midi#readme) を必ずご確認ください。

## v1.1.0

**ほかのプレイヤーへ聴かせる演奏と、合奏での音詰まりを抑える「公開演奏最適化」を追加しました。**

自分の画面では正常に鳴っていても、細かい音が続くと、ほかのプレイヤー側では演奏イベントが渋滞して少しずつ遅れていきます。休符へ入ったときに早送りのように追いつく場合もあります。この版では、作者環境で行った持続打鍵・和音・バースト・休符の実測結果をもとに、ゲームへ送る譜面を公開演奏向けに整理します。

### 公開演奏最適化（OFF／LIVE／SYNC）

メイン画面の電波ボタンを押すたびに、`OFF → LIVE → SYNC`の順で切り替わります。

- **OFF**：MIDIを極力そのまま演奏します。
- **LIVE**：原曲の音をできるだけ残しながら、高密度区間が続いたときだけ段階的に整理を強めます。ほかのプレイヤーへ聴かせるソロ演奏向けです。
- **SYNC**：LIVEより積極的に音を整理し、安定性を優先します。合奏、ドラム、高密度な曲向けです。

> [!WARNING]
> **LIVEは「きれいに聞こえること」を保証するモードではありません。**
> 原曲の再現度をなるべく残しながら、実測上の限界に対して**ギリギリ崩壊しない水準を狙う設定**です。曲や通信環境によってはLIVEでも遅延するため、その場合はSYNCを使用してください。

同時和音を構成音の数だけ重いとは扱いません。ゲーム内の音域切替によって1つの和音が複数回の打鍵へ分割される場合は、その追加コストも見積もります。残す音はメロディー、ベース、拍頭、長い音、強い音を優先し、残した音の開始時刻や音程を前後へずらすことはありません。

### 合奏中はSYNCを自動適用

合奏を予約した時点から、公開演奏最適化は一時的にSYNCへ切り替わります。保存済みの設定は書き換えず、合奏の終了・取消後に元のOFF／LIVE／SYNCへ戻ります。

合奏の開始時刻を揃えても、各奏者の音が聴衆へ届くまでのばらつきや、ゲーム側の処理上限はなくなりません。SYNCはその影響を抑えるための設定であり、すべての曲・人数・通信環境で無遅延を保証するものではありません。

### 実機で確認したゲーム側の限界

演奏していない第三者が聴く条件で、次の傾向を確認しました。

- 等間隔の単音は毎秒6打では遅延せず、毎秒8打から遅延が蓄積
- 毎秒12打の単音を60秒続けると、約60秒の譜面が約1分54秒かけて再生
- 同じ合計12音/秒でも、`4音同時 × 毎秒3回`は約60秒のまま終了
- 12打/秒の高密度区間は、0.5秒なら問題なくても、1秒以上続くと途中のリズムが変形
- 聴く側の受信バッファを345msから2000msへ増やすと、崩れ始める時点は遅くなるが、継続的な処理上限そのものは増えない

負荷は「総音符数」よりも、細かく連続する発音タイミングの数に強く影響されます。同時和音は比較的軽く、単音の高速連続、アルペジオ、ドラムロールは渋滞しやすい傾向があります。

これらは特定環境での実測値であり、ゲーム内部のパケットやキューを直接観測した結果ではありません。通信環境、ゲームサーバー、聴く側の端末やバッファ設定によって限界は変わります。

### 音域切替の安定化

- 公開演奏最適化では、音域の異なる同時音が別々の鍵盤表示へ分割されるコストを考慮するようにしました。
- SYNCでは、同じ音を鳴らせる場合に短時間の`L Shift`／`L Ctrl`往復を避け、比較的安定していた`>`／`<`を優先します。
- `>`または`<`を同じ方向へ連続して送る場合は、ゲーム側の取りこぼしを避けるため、通常より間隔を空けます。

### UIの変更

- 音域調整・公開演奏・表情演奏の3ボタンを小型化し、曲名・ノート数・トラック情報の表示幅を広げました。
- 曲情報を3行にし、スマートアレンジと公開演奏の要約を3行目へ分けました。2行目へ併記していたときは、
  ノート数・トラック数・音域・BPMと並んだ時点で表示幅を超え、行ごと省略されていました。
  ウィンドウの高さが11px高くなります。
- 公開演奏ボタンは`OFF / LIVE / SYNC`表示にしました。
- 小型ボタン上では、スマートアレンジの`MELODY`を`MELO`、表情演奏の`NATURAL`を`MID`と表示します。正式名と保存設定は変わりません。
- UIサイズ0.5～2.0倍と、プレイリストを開いた状態でも配置が崩れないよう調整しました。

### 検証

- 公開演奏の律速を切り分ける専用MIDIで、単音レート、同時和音、近接音、バースト長、休符による回復を実機測定しました。
- OFF／LIVE／SYNC、ピアノ／ドラム、再生速度、音域分割、合奏時の一時SYNCを自動テストへ追加しました。
- 自動テスト360件を通過しています。

### 既知の制限

- LIVE／SYNCはゲーム側の処理上限をなくす機能ではなく、すべての曲で無遅延を保証しません。
- 長時間ほとんど休符がない曲や、細かな単音・ドラムロールが続く曲は、SYNCでも音が省略されたり遅延したりする場合があります。
- 公開演奏最適化の基準値は作者環境での実測に基づきます。別回線・別サーバー・別端末で同じ境界になるとは限りません。
- ピアノC7～C8、ギター・ベース、音域補正の未検証範囲はv1.0.0から変わっていません。詳細はREADMEを参照してください。

### 不具合の報告

動作報告・不具合の報告は、READMEの「報告・お問い合わせ」からお願いします。
